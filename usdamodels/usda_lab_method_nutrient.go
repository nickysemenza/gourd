// Code generated by SQLBoiler 4.10.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package usdamodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UsdaLabMethodNutrient is an object representing the database table.
type UsdaLabMethodNutrient struct {
	ID          int      `boil:"id" json:"id" toml:"id" yaml:"id"`
	LabMethodID null.Int `boil:"lab_method_id" json:"lab_method_id,omitempty" toml:"lab_method_id" yaml:"lab_method_id,omitempty"`
	NutrientID  null.Int `boil:"nutrient_id" json:"nutrient_id,omitempty" toml:"nutrient_id" yaml:"nutrient_id,omitempty"`

	R *usdaLabMethodNutrientR `boil:"rel" json:"rel" toml:"rel" yaml:"rel"`
	L usdaLabMethodNutrientL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UsdaLabMethodNutrientColumns = struct {
	ID          string
	LabMethodID string
	NutrientID  string
}{
	ID:          "id",
	LabMethodID: "lab_method_id",
	NutrientID:  "nutrient_id",
}

var UsdaLabMethodNutrientTableColumns = struct {
	ID          string
	LabMethodID string
	NutrientID  string
}{
	ID:          "usda_lab_method_nutrient.id",
	LabMethodID: "usda_lab_method_nutrient.lab_method_id",
	NutrientID:  "usda_lab_method_nutrient.nutrient_id",
}

// Generated where

var UsdaLabMethodNutrientWhere = struct {
	ID          whereHelperint
	LabMethodID whereHelpernull_Int
	NutrientID  whereHelpernull_Int
}{
	ID:          whereHelperint{field: "\"usda_lab_method_nutrient\".\"id\""},
	LabMethodID: whereHelpernull_Int{field: "\"usda_lab_method_nutrient\".\"lab_method_id\""},
	NutrientID:  whereHelpernull_Int{field: "\"usda_lab_method_nutrient\".\"nutrient_id\""},
}

// UsdaLabMethodNutrientRels is where relationship names are stored.
var UsdaLabMethodNutrientRels = struct {
	LabMethod string
}{
	LabMethod: "LabMethod",
}

// usdaLabMethodNutrientR is where relationships are stored.
type usdaLabMethodNutrientR struct {
	LabMethod *UsdaLabMethod `boil:"LabMethod" json:"LabMethod" toml:"LabMethod" yaml:"LabMethod"`
}

// NewStruct creates a new relationship struct
func (*usdaLabMethodNutrientR) NewStruct() *usdaLabMethodNutrientR {
	return &usdaLabMethodNutrientR{}
}

// usdaLabMethodNutrientL is where Load methods for each relationship are stored.
type usdaLabMethodNutrientL struct{}

var (
	usdaLabMethodNutrientAllColumns            = []string{"id", "lab_method_id", "nutrient_id"}
	usdaLabMethodNutrientColumnsWithoutDefault = []string{"id"}
	usdaLabMethodNutrientColumnsWithDefault    = []string{"lab_method_id", "nutrient_id"}
	usdaLabMethodNutrientPrimaryKeyColumns     = []string{"id"}
	usdaLabMethodNutrientGeneratedColumns      = []string{}
)

type (
	// UsdaLabMethodNutrientSlice is an alias for a slice of pointers to UsdaLabMethodNutrient.
	// This should almost always be used instead of []UsdaLabMethodNutrient.
	UsdaLabMethodNutrientSlice []*UsdaLabMethodNutrient
	// UsdaLabMethodNutrientHook is the signature for custom UsdaLabMethodNutrient hook methods
	UsdaLabMethodNutrientHook func(context.Context, boil.ContextExecutor, *UsdaLabMethodNutrient) error

	usdaLabMethodNutrientQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	usdaLabMethodNutrientType                 = reflect.TypeOf(&UsdaLabMethodNutrient{})
	usdaLabMethodNutrientMapping              = queries.MakeStructMapping(usdaLabMethodNutrientType)
	usdaLabMethodNutrientPrimaryKeyMapping, _ = queries.BindMapping(usdaLabMethodNutrientType, usdaLabMethodNutrientMapping, usdaLabMethodNutrientPrimaryKeyColumns)
	usdaLabMethodNutrientInsertCacheMut       sync.RWMutex
	usdaLabMethodNutrientInsertCache          = make(map[string]insertCache)
	usdaLabMethodNutrientUpdateCacheMut       sync.RWMutex
	usdaLabMethodNutrientUpdateCache          = make(map[string]updateCache)
	usdaLabMethodNutrientUpsertCacheMut       sync.RWMutex
	usdaLabMethodNutrientUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var usdaLabMethodNutrientAfterSelectHooks []UsdaLabMethodNutrientHook

var usdaLabMethodNutrientBeforeInsertHooks []UsdaLabMethodNutrientHook
var usdaLabMethodNutrientAfterInsertHooks []UsdaLabMethodNutrientHook

var usdaLabMethodNutrientBeforeUpdateHooks []UsdaLabMethodNutrientHook
var usdaLabMethodNutrientAfterUpdateHooks []UsdaLabMethodNutrientHook

var usdaLabMethodNutrientBeforeDeleteHooks []UsdaLabMethodNutrientHook
var usdaLabMethodNutrientAfterDeleteHooks []UsdaLabMethodNutrientHook

var usdaLabMethodNutrientBeforeUpsertHooks []UsdaLabMethodNutrientHook
var usdaLabMethodNutrientAfterUpsertHooks []UsdaLabMethodNutrientHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UsdaLabMethodNutrient) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UsdaLabMethodNutrient) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UsdaLabMethodNutrient) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UsdaLabMethodNutrient) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UsdaLabMethodNutrient) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UsdaLabMethodNutrient) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UsdaLabMethodNutrient) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UsdaLabMethodNutrient) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UsdaLabMethodNutrient) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaLabMethodNutrientAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUsdaLabMethodNutrientHook registers your hook function for all future operations.
func AddUsdaLabMethodNutrientHook(hookPoint boil.HookPoint, usdaLabMethodNutrientHook UsdaLabMethodNutrientHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		usdaLabMethodNutrientAfterSelectHooks = append(usdaLabMethodNutrientAfterSelectHooks, usdaLabMethodNutrientHook)
	case boil.BeforeInsertHook:
		usdaLabMethodNutrientBeforeInsertHooks = append(usdaLabMethodNutrientBeforeInsertHooks, usdaLabMethodNutrientHook)
	case boil.AfterInsertHook:
		usdaLabMethodNutrientAfterInsertHooks = append(usdaLabMethodNutrientAfterInsertHooks, usdaLabMethodNutrientHook)
	case boil.BeforeUpdateHook:
		usdaLabMethodNutrientBeforeUpdateHooks = append(usdaLabMethodNutrientBeforeUpdateHooks, usdaLabMethodNutrientHook)
	case boil.AfterUpdateHook:
		usdaLabMethodNutrientAfterUpdateHooks = append(usdaLabMethodNutrientAfterUpdateHooks, usdaLabMethodNutrientHook)
	case boil.BeforeDeleteHook:
		usdaLabMethodNutrientBeforeDeleteHooks = append(usdaLabMethodNutrientBeforeDeleteHooks, usdaLabMethodNutrientHook)
	case boil.AfterDeleteHook:
		usdaLabMethodNutrientAfterDeleteHooks = append(usdaLabMethodNutrientAfterDeleteHooks, usdaLabMethodNutrientHook)
	case boil.BeforeUpsertHook:
		usdaLabMethodNutrientBeforeUpsertHooks = append(usdaLabMethodNutrientBeforeUpsertHooks, usdaLabMethodNutrientHook)
	case boil.AfterUpsertHook:
		usdaLabMethodNutrientAfterUpsertHooks = append(usdaLabMethodNutrientAfterUpsertHooks, usdaLabMethodNutrientHook)
	}
}

// One returns a single usdaLabMethodNutrient record from the query.
func (q usdaLabMethodNutrientQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UsdaLabMethodNutrient, error) {
	o := &UsdaLabMethodNutrient{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "usdamodels: failed to execute a one query for usda_lab_method_nutrient")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UsdaLabMethodNutrient records from the query.
func (q usdaLabMethodNutrientQuery) All(ctx context.Context, exec boil.ContextExecutor) (UsdaLabMethodNutrientSlice, error) {
	var o []*UsdaLabMethodNutrient

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "usdamodels: failed to assign all query results to UsdaLabMethodNutrient slice")
	}

	if len(usdaLabMethodNutrientAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UsdaLabMethodNutrient records in the query.
func (q usdaLabMethodNutrientQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: failed to count usda_lab_method_nutrient rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q usdaLabMethodNutrientQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "usdamodels: failed to check if usda_lab_method_nutrient exists")
	}

	return count > 0, nil
}

// LabMethod pointed to by the foreign key.
func (o *UsdaLabMethodNutrient) LabMethod(mods ...qm.QueryMod) usdaLabMethodQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.LabMethodID),
	}

	queryMods = append(queryMods, mods...)

	return UsdaLabMethods(queryMods...)
}

// LoadLabMethod allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (usdaLabMethodNutrientL) LoadLabMethod(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUsdaLabMethodNutrient interface{}, mods queries.Applicator) error {
	var slice []*UsdaLabMethodNutrient
	var object *UsdaLabMethodNutrient

	if singular {
		object = maybeUsdaLabMethodNutrient.(*UsdaLabMethodNutrient)
	} else {
		slice = *maybeUsdaLabMethodNutrient.(*[]*UsdaLabMethodNutrient)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &usdaLabMethodNutrientR{}
		}
		if !queries.IsNil(object.LabMethodID) {
			args = append(args, object.LabMethodID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &usdaLabMethodNutrientR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.LabMethodID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.LabMethodID) {
				args = append(args, obj.LabMethodID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`usda_lab_method`),
		qm.WhereIn(`usda_lab_method.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load UsdaLabMethod")
	}

	var resultSlice []*UsdaLabMethod
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice UsdaLabMethod")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for usda_lab_method")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for usda_lab_method")
	}

	if len(usdaLabMethodNutrientAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.LabMethod = foreign
		if foreign.R == nil {
			foreign.R = &usdaLabMethodR{}
		}
		foreign.R.LabMethodUsdaLabMethodNutrients = append(foreign.R.LabMethodUsdaLabMethodNutrients, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.LabMethodID, foreign.ID) {
				local.R.LabMethod = foreign
				if foreign.R == nil {
					foreign.R = &usdaLabMethodR{}
				}
				foreign.R.LabMethodUsdaLabMethodNutrients = append(foreign.R.LabMethodUsdaLabMethodNutrients, local)
				break
			}
		}
	}

	return nil
}

// SetLabMethod of the usdaLabMethodNutrient to the related item.
// Sets o.R.LabMethod to related.
// Adds o to related.R.LabMethodUsdaLabMethodNutrients.
func (o *UsdaLabMethodNutrient) SetLabMethod(ctx context.Context, exec boil.ContextExecutor, insert bool, related *UsdaLabMethod) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"usda_lab_method_nutrient\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"lab_method_id"}),
		strmangle.WhereClause("\"", "\"", 2, usdaLabMethodNutrientPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.LabMethodID, related.ID)
	if o.R == nil {
		o.R = &usdaLabMethodNutrientR{
			LabMethod: related,
		}
	} else {
		o.R.LabMethod = related
	}

	if related.R == nil {
		related.R = &usdaLabMethodR{
			LabMethodUsdaLabMethodNutrients: UsdaLabMethodNutrientSlice{o},
		}
	} else {
		related.R.LabMethodUsdaLabMethodNutrients = append(related.R.LabMethodUsdaLabMethodNutrients, o)
	}

	return nil
}

// RemoveLabMethod relationship.
// Sets o.R.LabMethod to nil.
// Removes o from all passed in related items' relationships struct.
func (o *UsdaLabMethodNutrient) RemoveLabMethod(ctx context.Context, exec boil.ContextExecutor, related *UsdaLabMethod) error {
	var err error

	queries.SetScanner(&o.LabMethodID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("lab_method_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.LabMethod = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.LabMethodUsdaLabMethodNutrients {
		if queries.Equal(o.LabMethodID, ri.LabMethodID) {
			continue
		}

		ln := len(related.R.LabMethodUsdaLabMethodNutrients)
		if ln > 1 && i < ln-1 {
			related.R.LabMethodUsdaLabMethodNutrients[i] = related.R.LabMethodUsdaLabMethodNutrients[ln-1]
		}
		related.R.LabMethodUsdaLabMethodNutrients = related.R.LabMethodUsdaLabMethodNutrients[:ln-1]
		break
	}
	return nil
}

// UsdaLabMethodNutrients retrieves all the records using an executor.
func UsdaLabMethodNutrients(mods ...qm.QueryMod) usdaLabMethodNutrientQuery {
	mods = append(mods, qm.From("\"usda_lab_method_nutrient\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"usda_lab_method_nutrient\".*"})
	}

	return usdaLabMethodNutrientQuery{q}
}

// FindUsdaLabMethodNutrient retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUsdaLabMethodNutrient(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*UsdaLabMethodNutrient, error) {
	usdaLabMethodNutrientObj := &UsdaLabMethodNutrient{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"usda_lab_method_nutrient\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, usdaLabMethodNutrientObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "usdamodels: unable to select from usda_lab_method_nutrient")
	}

	if err = usdaLabMethodNutrientObj.doAfterSelectHooks(ctx, exec); err != nil {
		return usdaLabMethodNutrientObj, err
	}

	return usdaLabMethodNutrientObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UsdaLabMethodNutrient) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("usdamodels: no usda_lab_method_nutrient provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(usdaLabMethodNutrientColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	usdaLabMethodNutrientInsertCacheMut.RLock()
	cache, cached := usdaLabMethodNutrientInsertCache[key]
	usdaLabMethodNutrientInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			usdaLabMethodNutrientAllColumns,
			usdaLabMethodNutrientColumnsWithDefault,
			usdaLabMethodNutrientColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(usdaLabMethodNutrientType, usdaLabMethodNutrientMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(usdaLabMethodNutrientType, usdaLabMethodNutrientMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"usda_lab_method_nutrient\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"usda_lab_method_nutrient\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "usdamodels: unable to insert into usda_lab_method_nutrient")
	}

	if !cached {
		usdaLabMethodNutrientInsertCacheMut.Lock()
		usdaLabMethodNutrientInsertCache[key] = cache
		usdaLabMethodNutrientInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UsdaLabMethodNutrient.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UsdaLabMethodNutrient) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	usdaLabMethodNutrientUpdateCacheMut.RLock()
	cache, cached := usdaLabMethodNutrientUpdateCache[key]
	usdaLabMethodNutrientUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			usdaLabMethodNutrientAllColumns,
			usdaLabMethodNutrientPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("usdamodels: unable to update usda_lab_method_nutrient, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"usda_lab_method_nutrient\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, usdaLabMethodNutrientPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(usdaLabMethodNutrientType, usdaLabMethodNutrientMapping, append(wl, usdaLabMethodNutrientPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: unable to update usda_lab_method_nutrient row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: failed to get rows affected by update for usda_lab_method_nutrient")
	}

	if !cached {
		usdaLabMethodNutrientUpdateCacheMut.Lock()
		usdaLabMethodNutrientUpdateCache[key] = cache
		usdaLabMethodNutrientUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q usdaLabMethodNutrientQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: unable to update all for usda_lab_method_nutrient")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: unable to retrieve rows affected for usda_lab_method_nutrient")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UsdaLabMethodNutrientSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("usdamodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usdaLabMethodNutrientPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"usda_lab_method_nutrient\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, usdaLabMethodNutrientPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: unable to update all in usdaLabMethodNutrient slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: unable to retrieve rows affected all in update all usdaLabMethodNutrient")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UsdaLabMethodNutrient) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("usdamodels: no usda_lab_method_nutrient provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(usdaLabMethodNutrientColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	usdaLabMethodNutrientUpsertCacheMut.RLock()
	cache, cached := usdaLabMethodNutrientUpsertCache[key]
	usdaLabMethodNutrientUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			usdaLabMethodNutrientAllColumns,
			usdaLabMethodNutrientColumnsWithDefault,
			usdaLabMethodNutrientColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			usdaLabMethodNutrientAllColumns,
			usdaLabMethodNutrientPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("usdamodels: unable to upsert usda_lab_method_nutrient, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(usdaLabMethodNutrientPrimaryKeyColumns))
			copy(conflict, usdaLabMethodNutrientPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"usda_lab_method_nutrient\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(usdaLabMethodNutrientType, usdaLabMethodNutrientMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(usdaLabMethodNutrientType, usdaLabMethodNutrientMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "usdamodels: unable to upsert usda_lab_method_nutrient")
	}

	if !cached {
		usdaLabMethodNutrientUpsertCacheMut.Lock()
		usdaLabMethodNutrientUpsertCache[key] = cache
		usdaLabMethodNutrientUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UsdaLabMethodNutrient record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UsdaLabMethodNutrient) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("usdamodels: no UsdaLabMethodNutrient provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), usdaLabMethodNutrientPrimaryKeyMapping)
	sql := "DELETE FROM \"usda_lab_method_nutrient\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: unable to delete from usda_lab_method_nutrient")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: failed to get rows affected by delete for usda_lab_method_nutrient")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q usdaLabMethodNutrientQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("usdamodels: no usdaLabMethodNutrientQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: unable to delete all from usda_lab_method_nutrient")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: failed to get rows affected by deleteall for usda_lab_method_nutrient")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UsdaLabMethodNutrientSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(usdaLabMethodNutrientBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usdaLabMethodNutrientPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"usda_lab_method_nutrient\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, usdaLabMethodNutrientPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: unable to delete all from usdaLabMethodNutrient slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "usdamodels: failed to get rows affected by deleteall for usda_lab_method_nutrient")
	}

	if len(usdaLabMethodNutrientAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UsdaLabMethodNutrient) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUsdaLabMethodNutrient(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UsdaLabMethodNutrientSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UsdaLabMethodNutrientSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usdaLabMethodNutrientPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"usda_lab_method_nutrient\".* FROM \"usda_lab_method_nutrient\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, usdaLabMethodNutrientPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "usdamodels: unable to reload all in UsdaLabMethodNutrientSlice")
	}

	*o = slice

	return nil
}

// UsdaLabMethodNutrientExists checks if the UsdaLabMethodNutrient row exists.
func UsdaLabMethodNutrientExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"usda_lab_method_nutrient\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "usdamodels: unable to check if usda_lab_method_nutrient exists")
	}

	return exists, nil
}
