// Code generated by SQLBoiler 4.10.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UsdaFoundationFood is an object representing the database table.
type UsdaFoundationFood struct {
	FDCID     int         `boil:"fdc_id" json:"fdc_id" toml:"fdc_id" yaml:"fdc_id"`
	NDBNumber null.Int    `boil:"ndb_number" json:"ndb_number,omitempty" toml:"ndb_number" yaml:"ndb_number,omitempty"`
	Footnote  null.String `boil:"footnote" json:"footnote,omitempty" toml:"footnote" yaml:"footnote,omitempty"`

	R *usdaFoundationFoodR `boil:"rel" json:"rel" toml:"rel" yaml:"rel"`
	L usdaFoundationFoodL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UsdaFoundationFoodColumns = struct {
	FDCID     string
	NDBNumber string
	Footnote  string
}{
	FDCID:     "fdc_id",
	NDBNumber: "ndb_number",
	Footnote:  "footnote",
}

var UsdaFoundationFoodTableColumns = struct {
	FDCID     string
	NDBNumber string
	Footnote  string
}{
	FDCID:     "usda_foundation_food.fdc_id",
	NDBNumber: "usda_foundation_food.ndb_number",
	Footnote:  "usda_foundation_food.footnote",
}

// Generated where

var UsdaFoundationFoodWhere = struct {
	FDCID     whereHelperint
	NDBNumber whereHelpernull_Int
	Footnote  whereHelpernull_String
}{
	FDCID:     whereHelperint{field: "\"usda_foundation_food\".\"fdc_id\""},
	NDBNumber: whereHelpernull_Int{field: "\"usda_foundation_food\".\"ndb_number\""},
	Footnote:  whereHelpernull_String{field: "\"usda_foundation_food\".\"footnote\""},
}

// UsdaFoundationFoodRels is where relationship names are stored.
var UsdaFoundationFoodRels = struct {
	FDC string
}{
	FDC: "FDC",
}

// usdaFoundationFoodR is where relationships are stored.
type usdaFoundationFoodR struct {
	FDC *UsdaFood `boil:"FDC" json:"FDC" toml:"FDC" yaml:"FDC"`
}

// NewStruct creates a new relationship struct
func (*usdaFoundationFoodR) NewStruct() *usdaFoundationFoodR {
	return &usdaFoundationFoodR{}
}

// usdaFoundationFoodL is where Load methods for each relationship are stored.
type usdaFoundationFoodL struct{}

var (
	usdaFoundationFoodAllColumns            = []string{"fdc_id", "ndb_number", "footnote"}
	usdaFoundationFoodColumnsWithoutDefault = []string{"fdc_id"}
	usdaFoundationFoodColumnsWithDefault    = []string{"ndb_number", "footnote"}
	usdaFoundationFoodPrimaryKeyColumns     = []string{"fdc_id"}
	usdaFoundationFoodGeneratedColumns      = []string{}
)

type (
	// UsdaFoundationFoodSlice is an alias for a slice of pointers to UsdaFoundationFood.
	// This should almost always be used instead of []UsdaFoundationFood.
	UsdaFoundationFoodSlice []*UsdaFoundationFood
	// UsdaFoundationFoodHook is the signature for custom UsdaFoundationFood hook methods
	UsdaFoundationFoodHook func(context.Context, boil.ContextExecutor, *UsdaFoundationFood) error

	usdaFoundationFoodQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	usdaFoundationFoodType                 = reflect.TypeOf(&UsdaFoundationFood{})
	usdaFoundationFoodMapping              = queries.MakeStructMapping(usdaFoundationFoodType)
	usdaFoundationFoodPrimaryKeyMapping, _ = queries.BindMapping(usdaFoundationFoodType, usdaFoundationFoodMapping, usdaFoundationFoodPrimaryKeyColumns)
	usdaFoundationFoodInsertCacheMut       sync.RWMutex
	usdaFoundationFoodInsertCache          = make(map[string]insertCache)
	usdaFoundationFoodUpdateCacheMut       sync.RWMutex
	usdaFoundationFoodUpdateCache          = make(map[string]updateCache)
	usdaFoundationFoodUpsertCacheMut       sync.RWMutex
	usdaFoundationFoodUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var usdaFoundationFoodAfterSelectHooks []UsdaFoundationFoodHook

var usdaFoundationFoodBeforeInsertHooks []UsdaFoundationFoodHook
var usdaFoundationFoodAfterInsertHooks []UsdaFoundationFoodHook

var usdaFoundationFoodBeforeUpdateHooks []UsdaFoundationFoodHook
var usdaFoundationFoodAfterUpdateHooks []UsdaFoundationFoodHook

var usdaFoundationFoodBeforeDeleteHooks []UsdaFoundationFoodHook
var usdaFoundationFoodAfterDeleteHooks []UsdaFoundationFoodHook

var usdaFoundationFoodBeforeUpsertHooks []UsdaFoundationFoodHook
var usdaFoundationFoodAfterUpsertHooks []UsdaFoundationFoodHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UsdaFoundationFood) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UsdaFoundationFood) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UsdaFoundationFood) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UsdaFoundationFood) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UsdaFoundationFood) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UsdaFoundationFood) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UsdaFoundationFood) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UsdaFoundationFood) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UsdaFoundationFood) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usdaFoundationFoodAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUsdaFoundationFoodHook registers your hook function for all future operations.
func AddUsdaFoundationFoodHook(hookPoint boil.HookPoint, usdaFoundationFoodHook UsdaFoundationFoodHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		usdaFoundationFoodAfterSelectHooks = append(usdaFoundationFoodAfterSelectHooks, usdaFoundationFoodHook)
	case boil.BeforeInsertHook:
		usdaFoundationFoodBeforeInsertHooks = append(usdaFoundationFoodBeforeInsertHooks, usdaFoundationFoodHook)
	case boil.AfterInsertHook:
		usdaFoundationFoodAfterInsertHooks = append(usdaFoundationFoodAfterInsertHooks, usdaFoundationFoodHook)
	case boil.BeforeUpdateHook:
		usdaFoundationFoodBeforeUpdateHooks = append(usdaFoundationFoodBeforeUpdateHooks, usdaFoundationFoodHook)
	case boil.AfterUpdateHook:
		usdaFoundationFoodAfterUpdateHooks = append(usdaFoundationFoodAfterUpdateHooks, usdaFoundationFoodHook)
	case boil.BeforeDeleteHook:
		usdaFoundationFoodBeforeDeleteHooks = append(usdaFoundationFoodBeforeDeleteHooks, usdaFoundationFoodHook)
	case boil.AfterDeleteHook:
		usdaFoundationFoodAfterDeleteHooks = append(usdaFoundationFoodAfterDeleteHooks, usdaFoundationFoodHook)
	case boil.BeforeUpsertHook:
		usdaFoundationFoodBeforeUpsertHooks = append(usdaFoundationFoodBeforeUpsertHooks, usdaFoundationFoodHook)
	case boil.AfterUpsertHook:
		usdaFoundationFoodAfterUpsertHooks = append(usdaFoundationFoodAfterUpsertHooks, usdaFoundationFoodHook)
	}
}

// One returns a single usdaFoundationFood record from the query.
func (q usdaFoundationFoodQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UsdaFoundationFood, error) {
	o := &UsdaFoundationFood{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for usda_foundation_food")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UsdaFoundationFood records from the query.
func (q usdaFoundationFoodQuery) All(ctx context.Context, exec boil.ContextExecutor) (UsdaFoundationFoodSlice, error) {
	var o []*UsdaFoundationFood

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to UsdaFoundationFood slice")
	}

	if len(usdaFoundationFoodAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UsdaFoundationFood records in the query.
func (q usdaFoundationFoodQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count usda_foundation_food rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q usdaFoundationFoodQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if usda_foundation_food exists")
	}

	return count > 0, nil
}

// FDC pointed to by the foreign key.
func (o *UsdaFoundationFood) FDC(mods ...qm.QueryMod) usdaFoodQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"fdc_id\" = ?", o.FDCID),
	}

	queryMods = append(queryMods, mods...)

	return UsdaFoods(queryMods...)
}

// LoadFDC allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (usdaFoundationFoodL) LoadFDC(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUsdaFoundationFood interface{}, mods queries.Applicator) error {
	var slice []*UsdaFoundationFood
	var object *UsdaFoundationFood

	if singular {
		object = maybeUsdaFoundationFood.(*UsdaFoundationFood)
	} else {
		slice = *maybeUsdaFoundationFood.(*[]*UsdaFoundationFood)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &usdaFoundationFoodR{}
		}
		args = append(args, object.FDCID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &usdaFoundationFoodR{}
			}

			for _, a := range args {
				if a == obj.FDCID {
					continue Outer
				}
			}

			args = append(args, obj.FDCID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`usda_food`),
		qm.WhereIn(`usda_food.fdc_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load UsdaFood")
	}

	var resultSlice []*UsdaFood
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice UsdaFood")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for usda_food")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for usda_food")
	}

	if len(usdaFoundationFoodAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.FDC = foreign
		if foreign.R == nil {
			foreign.R = &usdaFoodR{}
		}
		foreign.R.FDCUsdaFoundationFood = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.FDCID == foreign.FDCID {
				local.R.FDC = foreign
				if foreign.R == nil {
					foreign.R = &usdaFoodR{}
				}
				foreign.R.FDCUsdaFoundationFood = local
				break
			}
		}
	}

	return nil
}

// SetFDC of the usdaFoundationFood to the related item.
// Sets o.R.FDC to related.
// Adds o to related.R.FDCUsdaFoundationFood.
func (o *UsdaFoundationFood) SetFDC(ctx context.Context, exec boil.ContextExecutor, insert bool, related *UsdaFood) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"usda_foundation_food\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"fdc_id"}),
		strmangle.WhereClause("\"", "\"", 2, usdaFoundationFoodPrimaryKeyColumns),
	)
	values := []interface{}{related.FDCID, o.FDCID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.FDCID = related.FDCID
	if o.R == nil {
		o.R = &usdaFoundationFoodR{
			FDC: related,
		}
	} else {
		o.R.FDC = related
	}

	if related.R == nil {
		related.R = &usdaFoodR{
			FDCUsdaFoundationFood: o,
		}
	} else {
		related.R.FDCUsdaFoundationFood = o
	}

	return nil
}

// UsdaFoundationFoods retrieves all the records using an executor.
func UsdaFoundationFoods(mods ...qm.QueryMod) usdaFoundationFoodQuery {
	mods = append(mods, qm.From("\"usda_foundation_food\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"usda_foundation_food\".*"})
	}

	return usdaFoundationFoodQuery{q}
}

// FindUsdaFoundationFood retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUsdaFoundationFood(ctx context.Context, exec boil.ContextExecutor, fDCID int, selectCols ...string) (*UsdaFoundationFood, error) {
	usdaFoundationFoodObj := &UsdaFoundationFood{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"usda_foundation_food\" where \"fdc_id\"=$1", sel,
	)

	q := queries.Raw(query, fDCID)

	err := q.Bind(ctx, exec, usdaFoundationFoodObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from usda_foundation_food")
	}

	if err = usdaFoundationFoodObj.doAfterSelectHooks(ctx, exec); err != nil {
		return usdaFoundationFoodObj, err
	}

	return usdaFoundationFoodObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UsdaFoundationFood) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no usda_foundation_food provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(usdaFoundationFoodColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	usdaFoundationFoodInsertCacheMut.RLock()
	cache, cached := usdaFoundationFoodInsertCache[key]
	usdaFoundationFoodInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			usdaFoundationFoodAllColumns,
			usdaFoundationFoodColumnsWithDefault,
			usdaFoundationFoodColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(usdaFoundationFoodType, usdaFoundationFoodMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(usdaFoundationFoodType, usdaFoundationFoodMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"usda_foundation_food\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"usda_foundation_food\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into usda_foundation_food")
	}

	if !cached {
		usdaFoundationFoodInsertCacheMut.Lock()
		usdaFoundationFoodInsertCache[key] = cache
		usdaFoundationFoodInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UsdaFoundationFood.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UsdaFoundationFood) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	usdaFoundationFoodUpdateCacheMut.RLock()
	cache, cached := usdaFoundationFoodUpdateCache[key]
	usdaFoundationFoodUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			usdaFoundationFoodAllColumns,
			usdaFoundationFoodPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update usda_foundation_food, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"usda_foundation_food\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, usdaFoundationFoodPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(usdaFoundationFoodType, usdaFoundationFoodMapping, append(wl, usdaFoundationFoodPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update usda_foundation_food row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for usda_foundation_food")
	}

	if !cached {
		usdaFoundationFoodUpdateCacheMut.Lock()
		usdaFoundationFoodUpdateCache[key] = cache
		usdaFoundationFoodUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q usdaFoundationFoodQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for usda_foundation_food")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for usda_foundation_food")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UsdaFoundationFoodSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usdaFoundationFoodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"usda_foundation_food\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, usdaFoundationFoodPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in usdaFoundationFood slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all usdaFoundationFood")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UsdaFoundationFood) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no usda_foundation_food provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(usdaFoundationFoodColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	usdaFoundationFoodUpsertCacheMut.RLock()
	cache, cached := usdaFoundationFoodUpsertCache[key]
	usdaFoundationFoodUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			usdaFoundationFoodAllColumns,
			usdaFoundationFoodColumnsWithDefault,
			usdaFoundationFoodColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			usdaFoundationFoodAllColumns,
			usdaFoundationFoodPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert usda_foundation_food, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(usdaFoundationFoodPrimaryKeyColumns))
			copy(conflict, usdaFoundationFoodPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"usda_foundation_food\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(usdaFoundationFoodType, usdaFoundationFoodMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(usdaFoundationFoodType, usdaFoundationFoodMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert usda_foundation_food")
	}

	if !cached {
		usdaFoundationFoodUpsertCacheMut.Lock()
		usdaFoundationFoodUpsertCache[key] = cache
		usdaFoundationFoodUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UsdaFoundationFood record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UsdaFoundationFood) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UsdaFoundationFood provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), usdaFoundationFoodPrimaryKeyMapping)
	sql := "DELETE FROM \"usda_foundation_food\" WHERE \"fdc_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from usda_foundation_food")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for usda_foundation_food")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q usdaFoundationFoodQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no usdaFoundationFoodQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from usda_foundation_food")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for usda_foundation_food")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UsdaFoundationFoodSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(usdaFoundationFoodBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usdaFoundationFoodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"usda_foundation_food\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, usdaFoundationFoodPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from usdaFoundationFood slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for usda_foundation_food")
	}

	if len(usdaFoundationFoodAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UsdaFoundationFood) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUsdaFoundationFood(ctx, exec, o.FDCID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UsdaFoundationFoodSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UsdaFoundationFoodSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usdaFoundationFoodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"usda_foundation_food\".* FROM \"usda_foundation_food\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, usdaFoundationFoodPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UsdaFoundationFoodSlice")
	}

	*o = slice

	return nil
}

// UsdaFoundationFoodExists checks if the UsdaFoundationFood row exists.
func UsdaFoundationFoodExists(ctx context.Context, exec boil.ContextExecutor, fDCID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"usda_foundation_food\" where \"fdc_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, fDCID)
	}
	row := exec.QueryRowContext(ctx, sql, fDCID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if usda_foundation_food exists")
	}

	return exists, nil
}
